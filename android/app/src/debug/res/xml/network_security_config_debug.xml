<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!-- Debug builds can talk to any host (Metro, local gateways, etc.) -->
    <base-config cleartextTrafficPermitted="true" />
</network-security-config>
